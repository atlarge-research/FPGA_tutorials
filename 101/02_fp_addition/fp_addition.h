#include "data.h"

/*
    Return the sum of the two floating point number given as argument
*/
FP_TYPE fp_add(FP_TYPE, FP_TYPE);

/*
    Sums all element over the array, and adds to the starting value
*/
FP_TYPE fp_add_array(FP_TYPE start);